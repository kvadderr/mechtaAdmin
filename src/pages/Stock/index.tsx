import { Button, Checkbox, Form, Input, Flex } from 'antd';
import type { FormProps } from 'antd';
type FieldType = {
  username?: string;
  password?: string;
  remember?: string;
};

const Stock = () => {
  return 
}

export default Stock